import React from 'react'
import Box from '../src/components/Box'
import theme from '../src/theme/theme'

const HomeScreen = () => {
  return (
    <Box tag={"main"} styleSheet={{fonFamily: theme.typography.fontFamily}}>
        oi
    </Box>
  )
}

export default HomeScreen